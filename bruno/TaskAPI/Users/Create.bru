meta {
  name: Create
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/users
  body: json
  auth: none
}

headers {
  content-type: application/json
}

body:json {
  {
    "name": "{{userName}}",
    "email": "{{email}}",
    "password": "{{password}}"
  }
}
