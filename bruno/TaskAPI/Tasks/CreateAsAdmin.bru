meta {
  name: CreateAsAdmin
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/tasks
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminAccessToken}}
}

headers {
  content-type: application/json
}

body:json {
  {
    "title": "{{taskTitle}}",
    "description": "{{taskDescription}}",
    "tags": [
      "{{taskTag}}"
    ],
    "userEmail": "{{taskUserEmail}}",
    "responsibleEmail": "{{taskResponsibleEmail}}"
  }
}
